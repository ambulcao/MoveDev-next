import React from 'react';

import { Container } from './styles';

const Header: React.FC = () => {
    return (
        <Container>
            Pomodoro Developer
        </Container>
    );
};

export default Header;